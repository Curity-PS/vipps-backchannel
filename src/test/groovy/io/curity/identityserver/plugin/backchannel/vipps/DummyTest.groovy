package io.curity.identityserver.plugin.backchannel.vipps

import spock.lang.Specification

/**
 * Dummy test to ensure the test task doesn't fail when no unit tests exist.
 * Unit tests can be added here as needed.
 */
class DummyTest extends Specification {

    def "dummy test passes"() {
        expect:
        true
    }
}
